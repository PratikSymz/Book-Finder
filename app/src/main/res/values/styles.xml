<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- wrapContent Style -->
    <style name="wrapContent">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <!-- matchParent Style -->
    <style name="matchParent">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <!-- Style for RecyclerView -->
    <style name="recycler" parent="matchParent">
    </style>

    <!-- Style for RecyclerView Layout-->
    <style name="recycler_view_layout" parent="matchParent">
        <item name="android:layout_below">@id/search_card_view</item>

    </style>

    <!-- Style for SearchView -->
    <style name="searchBarCardViewStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/dimen_55dp</item>
        <item name="android:layout_margin">@dimen/dimen_5dp</item>
        <item name="android:layout_alignParentTop">true</item>
        <item name="android:queryHint">"Search Book"</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
    </style>

    <!-- Style for RatingBar -->
    <style name="ratingBarStyle" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/grey</item>
        <item name="colorControlActivated">@color/gold</item>
        <item name="android:padding">@dimen/dimen_2dp</item>
    </style>

    <!-- Style for CardView -->
    <style name="bookItemCardViewStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">10dp</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
    </style>

    <!-- Style for bookLayout RelativeLayout -->
    <style name="bookLayoutStyle" parent="matchParent">
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:padding">@dimen/dimen_2dp</item>
    </style>

    <!-- Style for textLayout LinearLayout -->
    <style name="textLayoutStyle" parent="wrapContent">
        <item name="android:orientation">vertical</item>
        <item name="android:layout_below">@id/book_thumbnail</item>
        <item name="android:layout_margin">@dimen/dimen_8dp</item>
        <item name="android:layout_gravity">center_horizontal</item>
    </style>

    <!-- Style for ImageView -->
    <style name="thumbnailStyle" parent="wrapContent">
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:scaleType">centerCrop</item>
    </style>

    <!-- Style for title TextView -->
    <style name="titleStyle" parent="wrapContent">
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">2</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:padding">@dimen/dimen_1dp</item>
        <item name="android:layout_gravity">center_horizontal</item>
    </style>

    <!-- Style for author TextView -->
    <style name="authorStyle" parent="wrapContent">
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
        <item name="android:textSize">@dimen/dimen_10sp</item>
        <item name="android:layout_gravity">center_horizontal</item>
    </style>

    <!-- Style for Home FloatingActionButton -->
    <style name="FABStyle" parent="wrapContent">
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:layout_alignParentRight">true</item>
        <item name="android:layout_marginRight">@dimen/dimen_16dp</item>
        <item name="android:layout_marginBottom">@dimen/dimen_16dp</item>
        <item name="android:clickable">true</item>
    </style>
</resources>